import cron from "node-cron";

export const scheduleCronJob = () => {
  //   if (typeof process !== "undefined" && process.hrtime) {
  //     // Use process.hrtime in the server-side context
  //     const hrTime = process.hrtime();
  //     cron.schedule("*/5 * * * *", () => {
  //       // The task to be executed on schedule
  //       console.log("Cron job executed!");
  //     });
  //     // Rest of the code using hrTime
  //   } else {
  //     // Handle the case when running in the client-side context
  //     console.log("Running in the client-side context");
  //   }
};
